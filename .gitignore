node_modules/
**/node_modules/
.env
.env.local
.env.*.local
.DS_Store
dist/
.next/
.turbo/
**/.next/
**/.turbo/
*.log
*.pid
artifacts/

# IDE / editor artifacts
.idea/
.codex-review/
.lh/
.vscode/

# TypeScript artifacts in source directories
**/tsconfig.tsbuildinfo
src/api/**/*.js
src/api/**/*.js.map
src/api/**/*.d.ts
src/shared/**/*.js
src/shared/**/*.js.map
src/shared/**/*.d.ts

# Mobile / desktop local build outputs (keep source, ignore generated state)
src/mobile/.expo/
src/mobile/android/.gradle/
src/mobile/android/build_output*.txt
src/mobile/ios/Pods/
src/mobile/ios_simulator*.png
src/desktop/dist/
src/desktop/src-tauri/target/
src/web/.next/
src/web/tsconfig.tsbuildinfo
src/desktop/.turbo/
src/mobile/.turbo/
src/web/.turbo/
src/shared/.turbo/
src/api/.turbo/
src/pitch/.turbo/

# Test coverage reports
**/coverage/

# Pitch deck build output is committed for GitHub Pages
# (docs/index.html + docs/assets/ are build artifacts that should be tracked)
!docs/
!docs/**
docs/**/.DS_Store

# Terraform
infra/terraform/.terraform/
infra/terraform/*.tfstate
infra/terraform/*.tfstate.backup
infra/terraform/terraform.tfvars
infra/terraform/.terraform.lock.hcl

# .config/ local state
.config/vercel/.vercel/

# Session exports and transient output (organvm CLI generates these)
exports/
output/
.claude/sessions/

# E2E test results
playwright-report/
test-results/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Generated by organvm/conductor and analysis
# NOTE: `.conductor/*`, not `.conductor/`. Git never descends into an excluded
# directory, so a `!` negation for a file inside `.conductor/` would be dead —
# excluding the *contents* is what makes the exception below reachable.
.conductor/*
# ...except the handoff pointer. CLAUDE.md instructs every agent to read
# `.conductor/active-handoff.md` FIRST, so ignoring it made the mandated entry
# point nonexistent in a fresh clone: the handoff lived only at a dated
# docs/audit/ path that nothing tracked pointed to, and an agent following the
# protocol correctly would find nothing and proceed blind. Keep this negation.
!.conductor/active-handoff.md
.opencode/
traceDir/
docs/evaluation/
# Large export files
docs/EXPORT-*.md
docs/audit/document-audit-*.md
data/

# Removed orphan AI session/prompt scaffolding (zero inbound references, not catalogued
# in docs/MANIFEST.md). These globs match ONLY the removed process-scaffolding classes —
# deliberately NOT the MANIFEST-catalogued product provenance kept in this repo: the
# brainstorm transcripts (DOC-BRN-*), the ai-session-audit doc, the gtm-role-prompts pack,
# and docs/planning/session-exports/ (which the GEMINI.md Session Review Protocol requires
# to stay tracked as audit exports). Raw session transcripts are on-demand, never committed.
docs/planning/premortem-transcript-*.md
docs/planning/planning--parallel-*-prompts--*.md
.codex/plans/**/*-prompts.md
.claude/plans/**/*ai-session-audit*.md

# Local verification scratch (see scripts/demo/README.md)
/src/api/.circle-smoke.mjs
/src/api/.column-audit.mjs
/src/api/.ccpa-sweep-check.mjs

# Committed terraform binaries removed 2026-07-30 (113MB); install via tfenv/brew
/terraform
/terraform_*.zip

# Local redis snapshot from verification runs
dump.rdb

# Cloudflare snapshot fixtures.
# Some machines carry a global gitignore rule for `public/` (core.excludesFile),
# which would silently drop these and leave a hosted snapshot with no data at all.
# Repository rules take precedence over the global file, so state the intent here.
!src/web/public/
!src/web/public/**
