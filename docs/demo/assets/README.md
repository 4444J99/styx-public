# Tour fallback video

`styx-tour-fallback.mp4` is a screen recording of the local, static `/tour` route. It contains no signed-in session, personal data, payment interaction, or claim that the live-stack gate passed. The recording is a travel fallback for the truth-labeled explanation; use the live demo only after its exact-head verifier passes.
